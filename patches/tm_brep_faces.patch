diff --git a/src/DataExchange/TKDEGLTF/RWGltf/RWGltf_CafWriter.cxx b/src/DataExchange/TKDEGLTF/RWGltf/RWGltf_CafWriter.cxx
index f5f50d0fa4..25150397f2 100644
--- a/src/DataExchange/TKDEGLTF/RWGltf/RWGltf_CafWriter.cxx
+++ b/src/DataExchange/TKDEGLTF/RWGltf/RWGltf_CafWriter.cxx
@@ -247,7 +247,8 @@ RWGltf_CafWriter::RWGltf_CafWriter(const TCollection_AsciiString& theFile,
       myToMergeFaces(false),
       myToSplitIndices16(false),
       myBinDataLen64(0),
-      myToParallel(false)
+      myToParallel(false),
+      myFaceIndex(0)
 {
   myCSTrsf.SetOutputLengthUnit(1.0); // meters
   myCSTrsf.SetOutputCoordinateSystem(RWMesh_CoordinateSystem_glTF);
@@ -480,6 +481,13 @@ void RWGltf_CafWriter::saveTriangleIndices(RWGltf_GltfFace&           theGltfFac
                                            const RWMesh_FaceIterator& theFaceIter,
                                            const std::shared_ptr<RWGltf_CafWriter::Mesh>& theMesh)
 {
+  // Invoke callback with face data before processing (triangle start = current count / 3)
+  if (myFaceDataCallback)
+  {
+    const Standard_Integer aTriStart = theGltfFace.Indices.Count / 3;
+    myFaceDataCallback(myFaceIndex++, aTriStart, theFaceIter.NbTriangles(), theFaceIter.Face());
+  }
+
   const Standard_Integer aNodeFirst = theGltfFace.NbIndexedNodes - theFaceIter.ElemLower();
   theGltfFace.NbIndexedNodes += theFaceIter.NbNodes();
   theGltfFace.Indices.Count += theFaceIter.NbTriangles() * 3;
@@ -840,6 +848,7 @@ bool RWGltf_CafWriter::writeBinData(const Handle(TDocStd_Document)& theDocument,
 
   myBinDataMap.Clear();
   myBinDataLen64 = 0;
+  myFaceIndex = 0;
 
   Message_ProgressScope aScope(theProgress,
                                "Write binary data",
diff --git a/src/DataExchange/TKDEGLTF/RWGltf/RWGltf_CafWriter.hxx b/src/DataExchange/TKDEGLTF/RWGltf/RWGltf_CafWriter.hxx
index 5c4f8e0be6..c467d87f4b 100644
--- a/src/DataExchange/TKDEGLTF/RWGltf/RWGltf_CafWriter.hxx
+++ b/src/DataExchange/TKDEGLTF/RWGltf/RWGltf_CafWriter.hxx
@@ -30,7 +30,9 @@
 #include <XCAFPrs_DocumentNode.hxx>
 #include <XCAFPrs_Style.hxx>
 #include <Poly_Triangle.hxx>
+#include <TopoDS_Face.hxx>
 
+#include <functional>
 #include <memory>
 
 class Message_ProgressRange;
@@ -154,6 +156,17 @@ public:
     myDracoParameters = theDracoParameters;
   }
 
+  //! Callback type for receiving per-face triangle data during mesh export.
+  //! Called for each TopoDS_Face with its face index, triangle start index, triangle count, and the face itself.
+  using FaceDataCallback = std::function<void(Standard_Integer theFaceIndex,
+                                              Standard_Integer theTriStart,
+                                              Standard_Integer theTriCount,
+                                              const TopoDS_Face& theFace)>;
+
+  //! Set callback for receiving per-face triangle mapping data.
+  //! When set, the callback is invoked during binary data export for each face.
+  void SetFaceDataCallback(const FaceDataCallback& theCallback) { myFaceDataCallback = theCallback; }
+
   //! Write glTF file and associated binary file.
   //! Triangulation data should be precomputed within shapes!
   //! @param[in] theDocument     input document
@@ -530,7 +543,9 @@ protected:
   std::vector<RWGltf_GltfBufferView>            myBuffViewsDraco;    //!< vector of buffers view with compression data
   Standard_Boolean                              myToParallel;        //!< flag to use multithreading; FALSE by default
                                             // clang-format on
-  RWGltf_DracoParameters myDracoParameters; //!< Draco parameters
+  RWGltf_DracoParameters myDracoParameters;  //!< Draco parameters
+  FaceDataCallback       myFaceDataCallback; //!< optional callback for per-face triangle data
+  Standard_Integer       myFaceIndex;        //!< current face index for callback
 };
 
 #endif // _RWGltf_CafWriter_HeaderFiler
